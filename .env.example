# 本地默认使用 mock，不需要真实 API Key
RIDEOPS_ENVIRONMENT=mock
RIDEOPS_DATABASE_PATH=./data/rideops.db
NEXT_PUBLIC_RIDEOPS_API_URL=http://127.0.0.1:8000
RIDEOPS_RAG_INDEX_PATH=./data/rag-index.db
RIDEOPS_EMBEDDING_PROVIDER=mock
EMBEDDING_BASE_URL=
EMBEDDING_API_KEY=
EMBEDDING_MODEL=text-embedding-3-small
# 默认不调用外部地图；设置为 amap_mcp 后使用高德官方远程 MCP
RIDEOPS_MAP_PROVIDER=mock
AMAP_API_KEY=
